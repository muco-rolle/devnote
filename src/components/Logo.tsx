import { Box } from '@chakra-ui/react';

export const Logo = () => {
    return (
        <Box
            fontFamily={`Pacifico, cursive`}
            fontWeight="bold"
            fontSize="1.5rem"
            color="gray.700"
        >
            devnote
        </Box>
    );
};
